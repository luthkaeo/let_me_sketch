---
description: Package the approved plan into an implementation task list (phase 90)
argument-hint: "<slug>"
---

Invoke the `prosona-handoff` skill. It indexes the existing phases into tasks grouped by
journey — it does not decide anything new.

`$1` is the project slug. Requires `60_review` approved.
