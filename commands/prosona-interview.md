---
description: Interview the planner once to capture their decision principles
argument-hint: "[planner name]"
---

Invoke the `prosona-interview` skill. It writes `.prosona/planner-persona.md`, which
every later phase reads.

`$1` names the planner; omitted, ask for it as the first question. An interview already
in progress resumes from the last framework tag in `## Raw`.
