---
description: Write the service rules, exceptions, and decision rights (phase 20, GATE 2)
argument-hint: "<slug>"
---

Invoke the `prosona-policy` skill. Rules come before the journey — what the service
allows and refuses decides which paths can exist.

`$1` is the project slug. Requires `10_frame` approved.
