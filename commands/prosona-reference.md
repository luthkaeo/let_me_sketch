---
description: Collect same-domain Mobbin precedent for the mapped journey (phase 40)
argument-hint: "<slug>"
---

Invoke the `prosona-reference` skill. Only the `reference-scout` agent touches Mobbin;
its image-heavy responses stay out of the planning context.

`$1` is the project slug. No Mobbin connection is not a failure — the phase records
`신규 설계` and continues.
