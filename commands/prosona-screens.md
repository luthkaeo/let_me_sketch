---
description: Specify screens, states, and copy structure (phase 50, GATE 3)
argument-hint: "<slug>"
---

Invoke the `prosona-screens` skill. Each screen ships as a markdown/JSON pair and is
checked with `node scripts/check-screens.mjs`.

`$1` is the project slug.
