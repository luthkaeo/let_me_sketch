---
description: Map each persona's journey through the service (phase 30, GATE 2)
argument-hint: "<slug>"
---

Invoke the `prosona-journey` skill. It refuses to run until `10_frame` is approved and
says so rather than improvising the personas.

`$1` is the project slug.
