---
description: Test the drafted screens against every persona (phase 60, GATE 4)
argument-hint: "<slug>"
---

Invoke the `prosona-review` skill. One isolated `virtual-user` per persona, plus a
dependency audit and a reference-gap pass. Loopback to screens is capped at two rounds.

`$1` is the project slug.
