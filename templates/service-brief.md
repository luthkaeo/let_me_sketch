# 서비스 브리프 — <서비스/기능 이름>

> 모드: `new` | `improve` — `improve`면 `## 현재 상태`와 `## 바꾸려는 이유`가 필수다.
> 측정한 값에는 반드시 **출처**를 적는다(파일:줄, 쿼리, 화면 캡처 등). 기억으로 쓴 수치는 수치가 아니다.

## 1. 누구를 위한 것인가

<추상적 집단("2030 직장인")이 아니라 특정 인물. 이 절이 비면 페이즈가 통과하지 않는다>

## 2. 무슨 고통인가

<빈도 / 강도 / 지금은 어떻게 때우고 있는가> — 수치가 있으면 수치로, 출처와 함께

## 3. 왜 지금인가

<무엇이 바뀌어서 가능해졌거나 필요해졌는가>

## 4. 10배 버전

<시간·비용 제약이 없다면 무엇을 만들 것인가>

## 5. MVP

<가설을 증명하는 최소 단위. 4번을 깎은 것이지 다른 제품이 아니다>

## 6. 안티 골

<명시적으로 만들지 않는 것. 기획자 페르소나의 `안티 골`을 먼저 확인한다>

## 7. 성공 지표

<느낌이 아니라 지표. "쓰기 편해진다"가 아니라 무엇이 몇에서 몇으로>

## 제약

| ID | 제약 | 출처 | 협상 가능? |
|---|---|---|---|
| C-001 | <제약 한 문장> | <파일:줄 / 사용자 발언 / 측정> | 예 / 아니오 |

이 표가 이후 모든 페이즈의 판정 기준이다. 여정·화면·QA가 여기에 대조해 모순을 잡는다.
각 행은 `registerNode(ledger, 'C-001', ...)`으로 결정 원장에 등록되며, 이후 결정의 `dependsOn`이 이 ID를 가리킨다.

## 현재 상태   ← `improve` 모드 필수

<지금 실제로 어떻게 동작하는가. 코드·화면에서 **측정**하고 출처를 단다. 기억으로 쓰지 않는다>

## 바꾸려는 이유   ← `improve` 모드 필수

<현재 상태의 무엇이 문제인가. 위 측정과 연결되어야 한다>

## 미해결

- <가정하고 넘어간 것. 없으면 `- 없음`>
